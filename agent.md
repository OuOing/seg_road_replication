# Seg-Road 复现项目协作规范

本文件记录本项目中用户与 AI 助手的协作方式、学习节奏、代码风格和笔记格式。后续对话、代码修改和学习记录都应尽量遵守这里的规则。

---

## 1. 对话与讲解风格

*   默认使用中文交流。
*   用户当前缺少深度学习、Python、NumPy、PyTorch 和论文复现基础，因此讲解时必须补充必要背景知识。
*   不要只给结论，要解释“为什么这样做”和“这行代码在干什么”。
*   每次学习尽量聚焦一个小主题，不要一次性展开太多概念。
*   遇到阻碍、权限问题、认证问题、网络问题或不确定选择时，先停下并向用户说明，不要长时间静默尝试。
*   做耗时操作前先说明目的；操作过程中需要简短同步当前进展。

---

## 2. 学习路线

本项目的学习顺序按从易到难推进：

1.  PCS 像素连通性结构：先理解 mask、8 方向连通性、数组平移、按位与。
2.  SRT 编码器：再学习 Transformer、自注意力、空间缩减、MixFFN。
3.  Seg-Road 完整模型：理解 Encoder、Decoder、分割分支、PCS 分支。
4.  损失函数与训练流程：学习 BCE、联合损失、Dataset、DataLoader、训练循环。
5.  指标与实验复现：学习 IoU、F1、Precision、Recall、推理与后处理。

讲解每个模块时，优先采用以下结构：

1.  先讲直觉：这个模块解决什么问题。
2.  再讲输入输出：数据形状是什么。
3.  再讲算法步骤：中间如何变化。
4.  最后讲代码语法：逐行解释关键 Python / NumPy / PyTorch 语法。

---

## 3. 笔记格式规范

笔记文件放在 `docs/` 目录下。

### 3.1 禁止使用 LaTeX 分隔符

当前 Markdown 渲染环境对数学公式支持不稳定。因此笔记中不要使用 LaTeX 分隔符，例如：

```text
美元符号包裹的行内公式
双美元符号包裹的块级公式
反斜杠小括号包裹的行内公式
反斜杠方括号包裹的块级公式
```

### 3.2 推荐的数学写法

短变量、函数名和代码概念使用反引号：

```markdown
`r`
`H x W`
`Target_con[d, y, x]`
`np.zeros(...)`
```

复杂公式使用代码块：

```text
Target_con[d, y, x] = Y[y, x] AND Y[y + dy_d, x + dx_d]
```

维度说明使用代码块：

```text
Y shape: H x W
PCS shape: 8 x H x W
```

需要解释符号时，用普通列表：

*   `H`：图像高度；
*   `W`：图像宽度；
*   `r`：检查连通性的像素间隔；
*   `AND`：两个位置同时为 1 时结果才为 1。

### 3.3 笔记内容要求

*   新概念第一次出现时，需要给出白话解释。
*   代码第一次出现时，需要解释关键语法。
*   每节结尾可以加入“本节记住一句话”或“下一节要学什么”。
*   示例优先使用小尺寸数组，例如 `5 x 5`，避免一开始就使用真实的 `512 x 512`。

---

## 4. 代码风格

*   代码应保持简洁，优先和现有文件风格一致。
*   Python 代码优先使用清晰变量名，不使用难懂缩写。
*   只有在逻辑不直观时才添加注释，注释要解释原因，不要重复代码本身。
*   不要引入不必要的复杂抽象。
*   修改代码后，优先运行和修改文件直接相关的最小验证命令。

---

## 5. Git 与项目文件

*   PDF 论文和本地虚拟环境不提交到 Git。
*   `.venv/`、`.DS_Store`、`docs/*.pdf` 应保持在 `.gitignore` 中。
*   提交前需要检查 `git status`，确认没有误提交大文件或本地临时文件。
*   如果需要推送远程，遇到认证、权限、邮箱暴露等问题时必须停下询问用户。

---

## 6. 当前项目目标

当前项目目标是复现论文：

```text
Seg-Road: A Segmentation Network for Road Extraction Based on Transformer and CNN with Connectivity Structures
```

当前阶段重点不是追求快速训练结果，而是先把论文结构、核心模块和基础代码能力学扎实。
